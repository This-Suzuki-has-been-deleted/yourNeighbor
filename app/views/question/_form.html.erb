<%= form_with(model: question, local: true) do |form| %>

  <div class="field">
    <%= form.label 'タイトル' %>
    <%= form.text_field :title, id: :question_title %>
  </div>
  <div class="field">
    <%= form.label '内　　容' %>
    <%= form.text_field :text, id: :question_text %>
  </div>

  <h3>※質問に回答された場合、他の親の参考とさせていただくため
    削除することが出来ません。よろしければ登録してください。</h3>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
