<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "YourNeighbor" %></title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.7.2/html5shiv.min.js" type="text/javascript"></script>
    <![endif]-->
  </head>
  <body>

    <div class="navbar navbar-default navbar-static-top">
      <div class="container">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
          <a class="navbar-brand" href="#">
				      <a class="navbar-brand" href="#">Your Neighbor</a>
			   </a>
        <div class="navbar-collapse collapse navbar-responsive-collapse">
          <ul class="nav navbar-nav">
            <li><%= link_to "遊び場マップ", maps_path  %></li>
            <li><%= link_to "コラム", columns_path  %></li>
            <li><%= link_to "質問一覧", questions_path  %></li>
            <li><%= link_to "質問作成", new_question_path  %></li>
          </ul>
          <% if user_signed_in? %>
            <% if(current_user.username.blank?) %>
              <p class="navbar-text">ようこそ HCSさん。</p>
            <% else %>
              <p class="navbar-text">ようこそ <a href=user_path class="navbar-text"><%= current_user.userName %></a> さん。</p>
            <% end %>
              <p class="navbar-right"><%= link_to "ログアウト", destroy_user_session_path, method: :delete  %></p>
          <% end %>
        </div>
      </div>
    </div>


    <div class="container">
      <div class="row">
        <div class="col-lg-9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
        <div class="col-lg-3">
          <div class="well sidebar-nav">
            <h3></h3>
            <ul class="nav nav-list">
              <li><%= link_to "新規会員登録", new_user_registration_path  %></li>
              <li><%= link_to "ログアウト", destroy_user_session_path, method: :delete  %></li>
              <li><%= link_to "会員情報変更", edit_user_registration_path  %></li>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2018</p>
      </footer>

    </div> <!-- /container -->

  </body>
</html>
